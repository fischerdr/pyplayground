---
# Simple playbook for basic testing
# Tests: Basic playbook structure, simple tasks, template usage
- hosts: all
  tasks:
    - name: Simple task
      command: echo hello

    - name: Template task
      template:
        src: config.j2
        dest: /etc/config.conf
