# ~/.gooserc

model_provider: ollama
model: codellama:7b-instruct

extensions:
  - git
  - filesystem

settings:
  # Prevent Goose from running shell commands
  allow_shell: false
  # Disallow outbound network by default
  allow_network: false
  # Keep Goose contained to /workspace
  workspace_root: /workspace
